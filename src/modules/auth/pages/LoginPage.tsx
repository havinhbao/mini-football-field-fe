import { LoginForm } from '@/modules/auth/components';
import { FC } from 'react';

const LoginPage: FC = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
